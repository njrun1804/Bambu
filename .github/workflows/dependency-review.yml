name: Dependency review advisory

on:
  pull_request:
    branches: [main]
  workflow_dispatch: {}

concurrency:
  group: dependency-review-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  dependency-review:
    uses: njrun1804-cc/engineering-control-plane/.github/workflows/dependency-review.yml@9484d1c6f25db3cf865a07f7775bf597d8940271
