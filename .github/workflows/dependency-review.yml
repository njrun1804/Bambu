name: Dependency review advisory

on:
  pull_request:
    branches: [main]
  workflow_dispatch: {}

concurrency:
  group: dependency-review-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  dependency-review:
    uses: njrun1804-cc/engineering-control-plane/.github/workflows/dependency-review.yml@6b9c098e0324ff83c26d97bb8738d9ebce25cd95
